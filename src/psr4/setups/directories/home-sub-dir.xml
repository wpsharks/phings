<?xml version="1.0" encoding="UTF-8" ?>

  <project>

    <!-- ============================================  -->
    <!-- Initialize `${_home_sub_dir}`. -->
    <!-- ============================================  -->

    <if>
      <and>
        <not>
          <available resource="${_home_sub_dir}" type="dir" />
        </not>
      </and>
      <then>
        <mkdir dir="${_home_sub_dir}" />
      </then>
    </if>

  </project>
