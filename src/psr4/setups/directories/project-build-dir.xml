<?xml version="1.0" encoding="UTF-8" ?>

<project>

  <!-- ============================================  -->
  <!-- Initialize `${project.basedir}/.~build`. -->
  <!-- ============================================  -->

  <if>
    <not>
      <available resource="${project.basedir}/.~build" type="dir" />
    </not>
    <then>
      <mkdir dir="${project.basedir}/.~build" />
    </then>
  </if>

</project>
