<?xml version="1.0" encoding="UTF-8" ?>

  <project>

    <!-- ============================================  -->
    <!-- Initialize `${project.basedir}/.~build/.~/pkg-dirs`. -->
    <!-- ============================================  -->

    <if>
      <not>
        <available resource="${project.basedir}/.~build/.~/pkg-dirs/for-zip-tgz" type="dir" />
      </not>
      <then>
        <mkdir dir="${project.basedir}/.~build/.~/pkg-dirs/for-zip-tgz" />
      </then>
    </if>

  </project>
